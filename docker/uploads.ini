; Upload limits — matches nginx client_max_body_size for the docs vhost.
; Files larger than upload_max_filesize must use the chunked /api/projects/{id}/uploads endpoint.
upload_max_filesize = 64M
post_max_size       = 80M
max_file_uploads    = 50
memory_limit        = 256M
